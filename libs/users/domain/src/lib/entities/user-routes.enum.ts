export enum RouteDomainPath {
  Profiles = 'profiles',
  Roles = 'roles',
  Permissions = 'permissions',
}

export enum RouteItemPath {
  New = 'new',
  Edit = 'edit',
  View = 'view',
}

export enum RouteDataType {
  Context,
}

export enum RouteItemContext {
  None = 'None',
  New = 'New',
  Edit = 'Edit',
  View = 'View',
}

export enum UsersRouteParam {
  ProfileId = 'profileId',
}

export enum UsersRouteQueryParam {
  Foo = 'foo',
}
